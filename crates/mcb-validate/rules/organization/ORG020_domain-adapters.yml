id: ORG020
name: Domain Adapters
category: organization
severity: error
description: Adapters belong in the infrastructure layer, not in the domain layer.
rationale: Domain layer should be pure and free of infrastructure concerns.
engine: path
filters:
  file_patterns:
    - "crates/mcb-domain/src/**/adapters/**/*.rs"
