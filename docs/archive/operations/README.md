# Archived Operations Documents

This directory preserves operations documents removed from the active docs tree.
The files are historical context only; current operational truth lives in
`AGENTS.md`, `Makefile`, `makefiles/dispatch.mk`, `scripts/lib/mcb.sh`,
`config/*.yaml`, and active docs under `docs/`.

## Files

- `INTEGRATION_TEST_SKIPPING.md.bak` - archived because active integration test
  guidance now lives in `docs/testing/INTEGRATION_TESTS.md` and the old page
  referenced removed helper paths.
