# Rust formatting configuration
edition = "2024"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Default"
# group_imports = "StdExternalCrate" # requires nightly rustfmt; formatting already applied
