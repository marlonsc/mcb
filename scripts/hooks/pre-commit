#!/usr/bin/env bash
# Installed by `make run WHAT=mcb-hooks`. Tier-1 fast gate via the private MCB
# handler in custom.mk. No bypass — fix the cause (AGENTS.md §3).
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec make run WHAT=mcb-hook-pre-commit
